{
  "name": "n2mu",
  "private": true,
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "migrate": "cd server && npm run migrate",
    "seed": "cd server && npm run seed",
    "build": "cd client && npm install --include=optional && npm run build && cd ../server && npm install",
    "start": "cd server && npx tsx src/index.ts",
    "postdeploy": "cd server && npx tsx migrations/run.ts && npx tsx src/seed/questions.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
